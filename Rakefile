# -*- ruby -*-

# desc "run tests"
# task :run_tests do
# 	require 'rspec/core/rake_task'
# 	RSpec::Core::RakeTask.new('spec')
# end

require 'rspec/core/rake_task'
RSpec::Core::RakeTask.new('spec')

desc "Run tests"
task :default => :spec

# vim: syntax=ruby
